/**
 * Ghost kitchen category slugs - excluded from print/TV menus.
 * These categories are kept in the CMS for historical data but should not appear
 * in customer-facing menu displays.
 */
export const HIDDEN_CATEGORY_SLUGS = ['blazing-hen', 'cajun-creation'];
