{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "ignoreCommand": "bash -c 'if [[ \"$VERCEL_ENV\" != \"production\" ]]; then echo \"Skipping preview build\" && exit 1; else exit 0; fi'",
  "env": {
    "DISABLE_AUTH_CHECK": "",
    "ALLOW_AUTH_BYPASS_IN_DEV": ""
  }
}
